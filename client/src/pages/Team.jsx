import { Typography, Box } from '@mui/material';
export default function Team() {
  return (
    <Box>
      <Typography variant="h2" sx={{ mb: 2 }}>Team</Typography>
      <Typography variant="body1">Add photos, roles, and bios here.</Typography>
    </Box>
  );
}
